Given existing Anki cards exported with a column containing audio files (identified by user-specified suffix, e.g. mp3), this collection calls OpenAI whisper on each audio file; writes the results to a specified jsonl file; and, optionally, provides a ChatGPT translation of the transcribed audio.

[WIP.]
